# Groq: https://console.groq.com/keys — free tier, fast inference
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# Optional: override paths (defaults work for repo layout)
# SAP_DATA_DIR=../sap-o2c-data
# SQLITE_PATH=./data/sap_o2c.db

